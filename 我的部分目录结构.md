# 我的板块（Memory + 知识库构建）目录结构文档

> 说明：本目录为本人负责的底层底座模块完整代码结构。所有文件在 `src/` 下，与团队其他模块的 `src/` 合并后形成完整项目。
>
> 最后更新：2026-07-23

---

## 一、完整目录树

```
src/
│
├── core/                               # 【核心类型与全局配置】
│   ├── __init__.py                     # 模块导出
│   ├── state.py                        # ResearchState + 所有TypedDict子类型定义
│   ├── models.py                       # Pydantic运行时校验模型 (ProjectMeta等)
│   ├── config.py                       # 全局配置（嵌入模型/切片参数/数据库连接串）
│   └── constants.py                    # 枚举常量（StageEnum, TaskStatusEnum, SourceTypeEnum等）
│
├── graph_rag/                          # 【GraphRAG双层知识库】
│   ├── __init__.py                     # 模块导出
│   ├── parser.py                       # 文档解析器（pdfplumber + nougat双引擎）
│   ├── chunker.py                      # 分层切片器（文档/章节/段落/句子四级）
│   ├── embedder.py                     # 嵌入模型统一封装（bge-large-zh + text-embedding-3-small）
│   ├── entity_extract.py              # LLM驱动的科研三元组抽取
│   ├── vector_store.py                 # Chroma向量库操作封装
│   ├── graph_store.py                  # Neo4j图数据库操作封装
│   ├── retriever.py                    # 混合检索器（向量+BM25 → RRF融合 → MMR重排序）
│   ├── sync_manager.py                 # 论文分块哈希增量同步管理
│   └── knowledge_base.py              # 知识库构建编排器（解析→切片→嵌入→入库 一体化）
│
├── memory/                             # 【分层记忆系统】
│   ├── __init__.py                     # 模块导出
│   ├── short_memory.py                 # 短期记忆三层压缩逻辑
│   ├── long_memory.py                  # 长期记忆读写（私有知识库交互）
│   ├── memory_manager.py              # 记忆统一编排（加载/压缩/持久化调度）
│   └── summary_llm.py                  # LLM摘要提示词模板与管理
│
├── controller/                         # 【LangGraph流程控制器】
│   ├── __init__.py                     # 模块导出
│   ├── workflow.py                     # StateGraph构建与节点注册
│   ├── router.py                       # 阶段流转路由（条件边判断逻辑）
│   ├── gates.py                        # 人工审批闸门（langgraph interrupt机制）
│   ├── error_handler.py               # 停滞检测 + REWORK/BLOCKED/FAILED处理分支
│   └── scheduler.py                    # 顶层调度器（启动/驱动/暂停/恢复/断点续跑）
│
├── api/                                # 【公共API层——供Agent和前端调用】
│   ├── __init__.py                     # 模块导出
│   ├── rag_api.py                      # RAG检索接口（global_retrieve等）
│   ├── memory_api.py                   # 记忆读写接口（load/save/compress）
│   ├── state_api.py                    # 状态查询/持久化接口（persist/resume）
│   └── sync_api.py                     # 论文同步接口（sync_user_paper_to_kb等）
│
├── agents/                             # 【Agent接口规范——供其他成员参考】
│   ├── __init__.py                     # 模块导出
│   └── agent_specs.py                  # 6个Agent函数签名 + 读写权限矩阵 + 调用约束
│
├── utils/                              # 【工具函数】
│   ├── __init__.py                     # 模块导出
│   ├── hash_utils.py                   # SHA256分片哈希计算与对比
│   ├── validators.py                   # 输入校验（ProjectMeta/EvidenceItem格式校验）
│   └── logger.py                       # 统一日志（阶段日志/Agent日志/错误日志/性能日志）
│
└── data/                               # 【静态数据——不参与代码运行】
    ├── base_literature/                # 全局公共知识库源数据
    │   ├── papers/                     # SSCI论文PDF文件
    │   ├── theories/                   # 理论框架文档
    │   └── scales/                     # 量表/测量工具文档
    └── prompts/                        # LLM提示词模板
        ├── entity_extract.txt          # 三元组抽取提示词
        ├── memory_summary.txt          # 记忆摘要提示词
        └── evidence_synthesis.txt      # 证据综合提示词
```

---

## 二、模块职责与对外接口一览

### 2.1 core/ —— 全局基础定义

| 文件 | 职责 | 被谁依赖 |
|------|------|---------|
| `state.py` | 定义 `ResearchState` 及所有 TypedDict 子类型 | 全系统所有模块 |
| `models.py` | Pydantic 运行时校验模型 | api/ 层入口校验 |
| `config.py` | 数据库连接、嵌入模型参数、切片配置 | graph_rag/, memory/, controller/ |
| `constants.py` | 阶段枚举、状态枚举、标签枚举 | 全系统 |

**这是全项目唯一的数据结构定义来源。** 其他成员导入类型时必须从此处引用。

### 2.2 graph_rag/ —— 知识库构建与检索

| 文件 | 对外暴露 | 内部依赖 |
|------|---------|---------|
| `parser.py` | `parse_paper(file_path) → ParsedDocument` | — |
| `chunker.py` | `chunk_document(doc) → List[Chunk]` | parser |
| `embedder.py` | `embed_texts(texts) → List[List[float]]` | — |
| `entity_extract.py` | `extract_triples(text) → List[Triple]` | — |
| `vector_store.py` | `add_vectors()`, `search_vectors()` | embedder |
| `graph_store.py` | `add_triples()`, `query_graph()` | — |
| `retriever.py` | `hybrid_search()`, `mmr_rerank()` | vector_store, graph_store |
| `sync_manager.py` | `sync_paper()`, `get_versions()`, `rollback()` | chunker, embedder, vector_store |
| `knowledge_base.py` | `build_kb()`, `index_paper()` | parser, chunker, embedder, vector_store, graph_store |

### 2.3 memory/ —— 分层记忆管理

| 文件 | 对外暴露 | 内部依赖 |
|------|---------|---------|
| `short_memory.py` | `compress_short_memory(state) → ShortMemorySummary` | summary_llm |
| `long_memory.py` | `load_long_term_memory()`, `save_evidence_to_long_memory()` | graph_rag.vector_store, graph_rag.graph_store |
| `memory_manager.py` | `init_memory()`, `step_memory()` | short_memory, long_memory |
| `summary_llm.py` | `generate_summary()`, `extract_pinned()` | — |

### 2.4 controller/ —— 流程调度

| 文件 | 对外暴露 | 内部依赖 |
|------|---------|---------|
| `workflow.py` | `build_research_workflow() → StateGraph` | 全部Agent节点, gates, router, error_handler |
| `router.py` | `stage_router(state) → str` | core.state |
| `gates.py` | `human_approval_node(state) → ResearchState` | core.state |
| `error_handler.py` | `detect_stagnation(state) → str`, `error_handler_node(state)` | core.state, memory |
| `scheduler.py` | `run_research_project()`, `resume_project()` | workflow, memory, api |

### 2.5 api/ —— 公共接口

| 文件 | 对外暴露的函数 | 谁调用 |
|------|--------------|--------|
| `rag_api.py` | `global_retrieve()` | 全部6个Agent |
| `memory_api.py` | `load_long_term_memory()`, `save_evidence_to_long_memory()`, `compress_short_memory()` | 调度器, 证据综述Agent |
| `state_api.py` | `persist_state()`, `resume_state()` | 调度器 |
| `sync_api.py` | `sync_user_paper_to_kb()`, `get_paper_versions()`, `rollback_paper_version()` | 前端, 用户操作 |

### 2.6 agents/ —— Agent规范

| 文件 | 内容 | 谁使用 |
|------|------|--------|
| `agent_specs.py` | 6个Agent的函数签名、读写权限矩阵、禁止操作列表 | 其他团队成员开发Agent时参考 |

### 2.7 utils/ —— 工具

| 文件 | 功能 |
|------|------|
| `hash_utils.py` | SHA256文本哈希、分片哈希对比、增量变更检测 |
| `validators.py` | ProjectMeta校验、EvidenceItem字段校验、ResearchState完整性检查 |
| `logger.py` | 结构化日志（JSON格式）、阶段追踪、Agent调用追踪、错误堆栈 |

---

## 三、模块间依赖关系图

```
                    ┌─────────────┐
                    │  core/      │  ← 零依赖，最底层
                    │  state.py   │
                    │  models.py  │
                    │  config.py  │
                    │  constants  │
                    └──────┬──────┘
                           │
            ┌──────────────┼──────────────┐
            │              │              │
     ┌──────▼──────┐ ┌────▼─────┐ ┌──────▼──────┐
     │  utils/     │ │ graph_rag│ │  memory/    │
     │  hash_utils │ │  /       │ │             │
     │  validators │ │ parser   │ │ 短/长记忆    │
     │  logger     │ │ chunker  │ │             │
     └──────┬──────┘ │ embedder │ └──────┬──────┘
            │        │ vector   │        │
            │        │ graph    │        │
            │        │ retriever│        │
            │        │ sync_mgr │        │
            │        └────┬─────┘        │
            │             │              │
            └──────────┼──┼──────────────┘
                       │  │
                 ┌─────▼──▼─────┐
                 │   api/       │  ← 统一对外接口
                 │   rag_api    │
                 │   memory_api │
                 │   state_api  │
                 │   sync_api   │
                 └──────┬───────┘
                        │
          ┌─────────────┼─────────────┐
          │             │             │
    ┌─────▼─────┐ ┌────▼────┐ ┌──────▼──────┐
    │controller/│ │agents/  │ │ 外部调用者    │
    │ workflow  │ │ specs   │ │ (Agent/前端) │
    │ router    │ │         │ │              │
    │ gates     │ └─────────┘ └──────────────┘
    │ error_h   │
    │ scheduler │
    └───────────┘
```

**依赖原则：**
- `core/` → 零依赖，全系统最底层
- `utils/` → 仅依赖 `core/`
- `graph_rag/` → 依赖 `core/` + `utils/`
- `memory/` → 依赖 `core/` + `graph_rag/`（调用 vector_store 和 graph_store）
- `api/` → 依赖 `graph_rag/` + `memory/` + `core/`
- `controller/` → 依赖 `api/` + `core/` + `memory/`（最上层编排层）
- `agents/` → 仅依赖 `core/`（纯规范定义，不依赖其他模块）

---

## 四、关键数据流路径

### 路径1：Agent检索文献
```
Agent调用 api/rag_api.global_retrieve()
  → graph_rag/retriever.hybrid_search()
    → graph_rag/vector_store.search_vectors()      [Chroma向量检索]
    → graph_rag/graph_store.query_graph()           [Neo4j图谱辅助]
  → graph_rag/retriever.mmr_rerank()               [去重重排序]
  → 返回 List[EvidenceItem]
```

### 路径2：论文同步入私有库
```
前端调用 api/sync_api.sync_user_paper_to_kb()
  → graph_rag/sync_manager.sync_paper()
    → graph_rag/parser.parse_paper()                [解析PDF/文本]
    → graph_rag/chunker.chunk_document()             [分层切片]
    → utils/hash_utils.compute_chunk_hashes()        [计算哈希]
    → graph_rag/sync_manager._compare_versions()     [对比旧版]
    → graph_rag/embedder.embed_texts()               [仅嵌入变化分片]
    → graph_rag/vector_store.add_vectors()           [写入Chroma]
    → graph_rag/graph_store.add_triples()            [写入Neo4j]
    → 返回同步报告
```

### 路径3：短期记忆压缩
```
每轮Agent执行完成后 (controller/scheduler)
  → api/memory_api(调用) memory/memory_manager.step_memory()
    → memory/short_memory.compress_short_memory()
      → 扫描 recent_rounds, 分类为 Pinned / Core / Process 三层
      → memory/summary_llm.generate_summary()       [LLM压缩Process层]
      → 合并 ShortMemorySummary
    → api/state_api.persist_state()                 [持久化]
```

### 路径4：项目启动与记忆恢复
```
用户打开项目
  → controller/scheduler.run_research_project()
    → api/memory_api.load_long_term_memory()
      → memory/long_memory.load_long_term_memory()
        → graph_rag/vector_store (读私有Chroma Collection)
        → graph_rag/graph_store  (读私有Neo4j子图)
    → 预填充 state.evidence_ledger + state.long_memory_refs
    → controller/scheduler 驱动工作流执行
```

---

## 五、外部依赖清单（requirements.txt）

```
# === 核心框架 ===
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# === 数据库 ===
chromadb>=0.5.0
py2neo>=2021.2.3
psycopg2-binary>=2.9.9
neo4j>=5.0.0

# === 文档解析 ===
pdfplumber>=0.10.0
nougat-ocr>=0.1.0           # 公式PDF解析(可选，有GPU时使用)
camelot-py>=0.11.0           # 表格抽取

# === 嵌入模型 ===
sentence-transformers>=2.7.0 # bge模型运行依赖
openai>=1.30.0               # text-embedding-3-small API

# === 数据校验 ===
pydantic>=2.0.0

# === 工具 ===
numpy>=1.24.0
tqdm>=4.66.0

# === 全文检索（可选）===
elasticsearch>=8.0.0         # 混合检索的BM25关键词通道
```

---

## 六、与团队其他成员的接口约定

### 6.1 Agent开发者需要知道的

1. **状态结构从 `src/core/state.py` 导入**，所有类型定义以此为准
2. **Agent函数签名**：`def xxx_agent_node(state: ResearchState) -> ResearchState`
3. **可写字段**：仅 `state["output_xxx"]`（对应自己Agent的分区）
4. **检索文献**：调用 `from src.api.rag_api import global_retrieve`
5. **不直连数据库**：禁止在Agent内部 import chromadb / neo4j / psycopg2

### 6.2 前端开发者需要知道的

1. **启动项目**：`POST /api/project/start` → 后端调用 `run_research_project()`
2. **同步论文**：`POST /api/project/{project_id}/sync_paper` → 后端调用 `sync_user_paper_to_kb()`
3. **人工审批**：`POST /api/project/{project_id}/approve` → 后端调用 `resume_project()`
4. **所有接口返回体**：统一为 `ResearchState` 的JSON序列化形式

### 6.3 数据库管理员需要知道的

- **Chroma**：持久化目录 `./chroma_data/`，每个项目一个独立 Collection
- **Neo4j**：每个项目一个独立子图，通过 `project_id` 属性过滤
- **PostgreSQL**：LangGraph checkpoint 表自动创建，需预先创建数据库和用户
- **无需Docker也可运行**：Chroma嵌入式、Neo4j社区版本地安装、PostgreSQL本地安装
