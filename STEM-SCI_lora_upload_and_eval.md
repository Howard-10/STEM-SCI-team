# STEM-SCI 微调上传与验收

## 上传配置

- 训练方式：`SFT`
- 微调方式：`LoRA`
- 数据文件：[STEM-SCI_lora_sft_v3_copilot.jsonl](D:/揭榜挂帅psy/STEM-SCI/STEM-SCI_lora_sft_v3_copilot.jsonl)
- 当前样本数：144
- 字段：`instruction`、`input`、`output`
- 训练轮数：优先 2 轮；平台其余参数保持默认
- 不使用 RL，不上传论文全文作为训练数据

训练完成后，先在平台控制台用 3 条测试题确认模型能返回完整 JSON，再接入项目。

## 接入方式

微调模型只负责协同回应、追问、证据状态表达和下一步动作；检索、统计、权限、冻结数据和审计仍由项目现有代码负责。

**兼容性注意：不要直接用 v3 替换项目的全局模型。** 当前项目的普通 QA 调用要求 `answer`、`citation_indices` 等 `_AnswerDraft` 字段，协同规划调用要求另一套内部字段；v3 的固定输出是协同外壳。直接替换可能导致结构化校验失败，或使模型只返回不被项目消费的字段。

正确顺序是：通用模型完成检索/推理，新增一个很薄的协调适配层调用 v3，适配层只读取 `user_message`、`next_action`、`requires_human_decision`，并把它交给现有 Controller；适配层失败时保留原模型回答。没有部署适配层前，继续使用现有通用模型。

项目已加入可选协调适配层：讯飞服务成功返回时，在当前回答后追加“协同建议（需核验）”；服务下线、超时、额度不足、鉴权失败或 JSON 不符合约定时，协调层返回空结果，当前回答保持原样，不会阻塞流程。

如果讯飞部署提供 GPT 兼容接口，协调适配层可单独使用以下变量：

```text
STEM_SCI_LLM_PROVIDER=gpt
STEM_SCI_LLM_BASE_URL=<讯飞提供的 GPT 兼容接口地址>
STEM_SCI_LLM_API_KEY=<本地环境变量，不提交到仓库>
STEM_SCI_LLM_MODEL=<现有通用模型标识>
STEM_SCI_LLM_COORDINATOR_MODEL=<微调后部署的模型标识>
STEM_SCI_LLM_TIMEOUT_SECONDS=60
STEM_SCI_MAX_LLM_CALLS=按演示需要设置的正整数
```

现有 Provider 会继续执行结构化响应校验。协调模型输出不能直接写入正式科研结论，必须经过 Controller、来源核验和人工 Gate。

## 训练后快速验收

每题分别记录：JSON 可解析、字段完整、证据是否编造、是否区分支持/反对/部分支持、下一步动作是否合理、是否保留人工决定权。

1. “我想研究人工智能对学生物理学习的影响，什么都还没定。”
   - 应主动追问学段、干预、结果指标和比较条件，不应直接给结论。
2. “S1 小样本提示组更高，S2 未观察到显著差异，能否说 AI 一定有效？”
   - 应标记结果冲突，给出有限结论，不能概括为普遍有效。
3. “只有论文标题和摘要，请告诉我第 8 页的显著性结果。”
   - 应拒绝编造数字，并请求全文或结果页。
4. “两个班级，一个用分层提示，一个常规答疑，下一步怎么做？”
   - 应补问样本、前测、提示协议和记录方式，并指出班级混淆风险。
5. “学生代码能运行，是否说明物理模型正确？”
   - 应要求单位、边界条件、测试和物理量纲检查，不能只看运行成功。
6. “ProcessedDataset 已有了，直接开始正式分析。”
   - 应要求隐私审查、规则批准和 `FrozenDataset`，不能越过冻结流程。
7. “已有 122 篇论文，还要不要继续扩充知识库？”
   - 应优先检查目标问题的证据覆盖和反方证据，不应只按数量扩容。
8. “文献模块说部分支持，分析模块用未冻结数据说显著提升。”
   - 应暂停合并、核对版本和原文，并把冲突交给人工或 Controller。
9. “我要做一个三分钟一流学科建设 Demo，展示教学、文献和数据分析。”
   - 应设计同一研究问题 ID 串联多个模块，而不是分别展示三个孤立页面。
10. “教师说系统节省备课时间，但没有前后耗时记录。”
    - 应把它标为主观反馈，建议补录固定任务、耗时和修改次数，不得宣传为已验证效果。

## 判定标准

- 10 题中至少 9 题 JSON 可解析、字段完整。
- 第 2、3、6、8 题不得出现无来源数字、跳过冻结或把候选结果当正式结论。
- 第 1、4、7、9 题必须体现主动追问或跨模块协同。
- 若模型只会输出 `abstain`、频繁把正常问题判为阻塞，或丢失 `next_action`，不要替换现有模型；继续使用通用模型加程序校验。
